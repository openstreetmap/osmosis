package com.bretth.osmosis.core.xml.v0_6;

import org.junit.Test;

/**
 * A simple test verifying the operation of the xml reader and writer tasks.
 * 
 * @author Brett Henderson
 */
public class XmlReaderWriterTest {
	/**
	 * A basic test reading and writing an osm file testing both reader and writer tasks.
	 */
	@Test
	public void testSimple() {
		//XmlReader xmlReader;
		//URL url;
		
		//xmlReader = new XmlReader(this.getClass().getResource("/data/v0_6/xml-task-tests-v0_6.osm").openConnection().);
		//url = getClass().getResource("/data/v0_6/xml-task-tests-v0_6.osm");
		//File file = new File(url.getFile());
		//url.toString();
		//file.toString();
	}
}
